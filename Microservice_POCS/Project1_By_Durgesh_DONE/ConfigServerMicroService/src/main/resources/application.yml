server:
  port: 8085
  
  
spring:
  application:
    name: CONFIG-SERVER
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Anurag9929/Springboot_Microservices_Configurations
          clone-on-start: true